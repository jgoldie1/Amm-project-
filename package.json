{
  "name": "tryamm-intelligence-mvp",
  "version": "1.2.0",
  "private": true,
  "description": "TryAMM Intelligence and Phase 2 platform integration MVP",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/platform.js && node --check services/payments.js && node --check services/stripe.js && node --check services/livekit.js && node --check services/claude.js && node --check services/meshy.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.57.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "livekit-server-sdk": "^2.13.3",
    "socket.io": "^4.8.1",
    "stripe": "^18.5.0"
  }
}
