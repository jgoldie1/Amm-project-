# TRYAMM production browser configuration (public values only)
VITE_SUPABASE_URL=https://fxluchtdfpediivhoksl.supabase.co
# Preferred modern browser key name used by the live TRYAMM persistence client.
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_y2OadDy1zy8QlWy-YAcdlg_uzAYMLzj
# Compatibility alias retained for existing modules/tests; same browser-safe publishable key.
VITE_SUPABASE_ANON_KEY=sb_publishable_y2OadDy1zy8QlWy-YAcdlg_uzAYMLzj
VITE_APP_URL=https://tryamm.online

# AMM Express backend (Render/staging/production)
# Used by HoloGPT / Stubbs AI and server-controlled actions.
VITE_API_URL=https://your-amm-backend.example.com

# SERVER-ONLY production variables — configure in Vercel, never commit values.
SUPABASE_SERVICE_ROLE_KEY=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
TRYAMM_CALL_CENTER_WEBHOOK_SECRET=
TRYAMM_INTERNAL_COMPLIANCE_SECRET=
TRYAMM_RETENTION_JOB_SECRET=
TRYAMM_RECORDING_ARCHIVE_SECRET=
TRYAMM_STT_ENDPOINT=
TRYAMM_STT_API_KEY=
TRYAMM_TTS_ENDPOINT=
TRYAMM_TTS_API_KEY=
OPENAI_API_KEY=

# Controlled live-call verification. Keep both server-only.
# Destination must be a number you own or are authorized to call.
TRYAMM_TEST_CALL_SECRET=
TRYAMM_TEST_CALL_DESTINATION=

# Optional live conversational voice bridge.
TRYAMM_MEDIA_STREAM_WSS_URL=
TRYAMM_CALL_CENTER_GREETING=Welcome to TRYAMM. Middleverse AI and a human agent can assist you.

# Without Supabase browser configuration, the existing game can still run in
# demo/mock-login mode, but Living Worlds Passport, cloud projects, workforce
# runs, publishing and App Store entitlements will not persist.
#
# The publishable Supabase key above is intentionally browser-safe. Never put
# the Supabase service-role key, Twilio Auth Token, or AI provider secret in
# any VITE_* variable because Vite exposes VITE_* values to browser bundles.