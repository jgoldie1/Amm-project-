import crypto from 'node:crypto';
import {requireUser} from '../_lib/security.js';
const clean=(v,max=96)=>String(v||'').trim().replace(/[^A-Za-z0-9_-]/g,'-').slice(0,max);
const b64=v=>Buffer.from(typeof v==='string'?v:JSON.stringify(v)).toString('base64url');
function mint({key,secret,identity,name,room,role}){const now=Math.floor(Date.now()/1000),header={alg:'HS256',typ:'JWT'},payload={iss:key,sub:identity,nbf:now-5,exp:now+6*60*60,name,metadata:JSON.stringify({role}),video:{roomJoin:true,room,canSubscribe:true,canPublish:role==='host',canPublishData:true}};const unsigned=`${b64(header)}.${b64(payload)}`;const sig=crypto.createHmac('sha256',secret).update(unsigned).digest('base64url');return `${unsigned}.${sig}`}
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const user=await requireUser(req,res);if(!user)return;const url=process.env.LIVEKIT_URL||process.env.LIVEKIT_WS_URL||'',key=process.env.LIVEKIT_API_KEY||'',secret=process.env.LIVEKIT_API_SECRET||'';if(!url||!key||!secret)return res.status(503).json({error:'LIVE infrastructure is not configured'});const room=clean(req.body?.room,96),role=req.body?.role==='host'?'host':'viewer',displayName=String(req.body?.displayName||user.email||'Player').trim().slice(0,80);if(room.length<3)return res.status(400).json({error:'Valid room is required'});return res.status(200).json({token:mint({key,secret,identity:user.id,name:displayName,room,role}),url,room,role,participant:user.id});}
