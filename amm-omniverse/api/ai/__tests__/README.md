# HoloGPT readiness tests

These tests protect provider selection and certification semantics. A production HoloGPT certification requires a real model response, while diagnostic/recovery mode stays degraded. The configured gateway model remains first priority and the owned/self-hosted-first switch remains available.
