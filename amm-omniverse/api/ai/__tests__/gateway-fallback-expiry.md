# Gateway fallback lifecycle

`inclusionai/ling-3.0-flash-sante-free` is a temporary fallback, not owned production capacity. `HOLOGPT_GATEWAY_MODEL` remains the configurable first choice and `HOLOGPT_SELFHOST_PREFERRED=true` preserves the owned/self-hosted-first path. Production certification must be rechecked whenever provider availability changes.
