const WORLD_BOUNDS={latMin:-85,latMax:85,lngMin:-180,lngMax:180};
const RESERVED_ZONES=['military','critical-infrastructure','private-residence','school-restricted'];
function clamp(n,min,max){return Math.min(max,Math.max(min,Number(n)||0));}
function coordinate(input={}){return{lat:clamp(input.lat,WORLD_BOUNDS.latMin,WORLD_BOUNDS.latMax),lng:clamp(input.lng,WORLD_BOUNDS.lngMin,WORLD_BOUNDS.lngMax),altitude:Math.max(0,Number(input.altitude)||0)}}
function createWorldSession(input={}){return{id:`earth_${Date.now()}`,userId:String(input.userId||'demo-user'),position:coordinate(input.position),realm:['metaverse','middleverse','multiverse'].includes(input.realm)?input.realm:'metaverse',provider:process.env.GOOGLE_MAPS_API_KEY?'google-maps-platform':'mock-earth',privacyMode:input.privacyMode!==false,status:'ready',createdAt:new Date().toISOString()}}
function teleport(session,input={}){const target=coordinate(input.target);const zone=String(input.zoneType||'public').toLowerCase();if(RESERVED_ZONES.includes(zone))throw new Error('Teleport target is restricted for privacy or safety.');return{...session,position:target,lastTeleport:{from:session.position,to:target,label:String(input.label||'Twin Earth destination'),mode:String(input.mode||'portal'),at:new Date().toISOString()}}}
function worldLayerConfig(){return{satellite:Boolean(process.env.GOOGLE_MAPS_API_KEY),photorealistic3DTiles:Boolean(process.env.GOOGLE_MAPS_API_KEY),routes:Boolean(process.env.GOOGLE_MAPS_API_KEY),places:Boolean(process.env.GOOGLE_MAPS_API_KEY),weather:'adapter-required',traffic:'adapter-required',animals:'procedural-and-licensed-data-only',privacy:['blur private coordinates','no live user location sharing by default','restricted-zone deny list','parental controls']}}
module.exports={createWorldSession,teleport,worldLayerConfig,coordinate};