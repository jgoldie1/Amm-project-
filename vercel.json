{
  "buildCommand": "cd amm-omniverse && npm install && npm run build",
  "outputDirectory": "amm-omniverse/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
