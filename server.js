const express = require("express");
const http = require("http");
const https = require("https");
const fs = require("fs");
const path = require("path");
const crypto = require("crypto");
const { Server } = require("socket.io");
const GAMEVERSE = require("./data/gameverse.json");
const PLATFORM_STATUS = require("./data/platform-status.json");
const GAMEOPS_POLICY = require("./data/gameops-policy.json");
const PLAY_SESSION_POLICY = require("./data/play-session-policy.json");
const QUANTUM_SPEED_ENGINE = require("./data/quantum-speed-engine.json");
const TRYAMM_SERVICES = require("./data/tryamm-services.json");
const CREATOR_UNIVERSE = require("./data/creator-universe.json");
const MONETIZATION = require("./data/monetization-packages.json");
const ECONOMIC_OPPORTUNITY = require("./data/economic-opportunity-hub.json");
const GLOBAL_MOBILITY_READINESS = require("./data/global-mobility-readiness.json");
const DELIVERY_ORCHESTRATION = require("./data/delivery-orchestration.json");
const OMNICARE_360 = require("./data/omnicare-360.json");
const HOLO_COMMERCIAL_STUDIO = require("./data/holo-commercial-studio.json");
const AMM_MOTION_HOLOFX = require("./data/amm-motion-holofx.json");
const ASSET_HOLOGRAM_MARKETPLACE = require("./data/asset-hologram-marketplace.json");
const DNA_PROTECTION = require("./data/dna-protection.json");
const ASSET_GENERATION_PRICING = require("./data/asset-generation-pricing.json");
const TRYAMM_ANALYTICS = require("./data/tryamm-analytics.json");
const { createPlaySessionManager } = require("./lib/play-session-manager");
const { createServicesHubManager } = require("./lib/services-hub-manager");
const { createEconomicOpportunityManager } = require("./lib/economic-opportunity-manager");
const { registerEconomicOpportunityRoutes } = require("./lib/economic-opportunity-routes");
const { createMobilityOnboardingManager } = require("./lib/mobility-onboarding-manager");
const { registerMobilityOnboardingRoutes } = require("./lib/mobility-onboarding-routes");
const { createOmniCare360Manager } = require("./lib/omnicare-360-manager");
const { registerOmniCare360Routes } = require("./lib/omnicare-360-routes");
const { createHoloCommercialManager } = require("./lib/holo-commercial-manager");
const { registerHoloCommercialRoutes } = require("./lib/holo-commercial-routes");
const { createAssetMarketplaceManager } = require("./lib/asset-marketplace-manager");
const { registerAssetMarketplaceRoutes } = require("./lib/asset-marketplace-routes");
const { createDnaProtectionManager } = require("./lib/dna-protection-manager");
const { registerDnaProtectionRoutes } = require("./lib/dna-protection-routes");
const { createCostOpsManager } = require("./lib/costops-manager");
const { registerCostOpsRoutes } = require("./lib/costops-routes");
const { createTryAmmAnalyticsManager } = require("./lib/tryamm-analytics-manager");
const { registerTryAmmAnalyticsRoutes } = require("./lib/tryamm-analytics-routes");
const { createGameVerseRuntimeManager } = require("./lib/gameverse-runtime-manager");
const assetPipeline = require("./lib/asset-pipeline-manager");
const app = express(); const server = http.createServer(app); const io = new Server(server);
const playSessions = createPlaySessionManager({ gameverse: GAMEVERSE, io });
const servicesHub = createServicesHubManager({ servicesManifest: TRYAMM_SERVICES, io });
const economicOpportunity = createEconomicOpportunityManager({ manifest: ECONOMIC_OPPORTUNITY, io });
const mobilityOnboarding = createMobilityOnboardingManager({ readiness: GLOBAL_MOBILITY_READINESS, io });
const omniCare360 = createOmniCare360Manager({ manifest: OMNICARE_360, io });
const holoCommercial = createHoloCommercialManager({ manifest: HOLO_COMMERCIAL_STUDIO, motionManifest: AMM_MOTION_HOLOFX, io });
const dnaProtection = createDnaProtectionManager({ manifest: DNA_PROTECTION, io });
const assetMarketplace = createAssetMarketplaceManager({ manifest: ASSET_HOLOGRAM_MARKETPLACE, io, dnaProtection });
const costOps = createCostOpsManager({ pricing: ASSET_GENERATION_PRICING, io });
const tryAmmAnalytics = createTryAmmAnalyticsManager({ manifest: TRYAMM_ANALYTICS, io });
const gameRuntime = createGameVerseRuntimeManager({ io });
const SITE_URL = (process.env.SITE_URL || "https://tryamm.online").replace(/\/$/, "");
const AUDIT_LOG_PATH = process.env.GAMEOPS_LOG_PATH || path.join(process.cwd(), "runtime", "tryamm-audit.jsonl");
const gameOpsIncidents = [];
function appendAudit(record) { try { fs.mkdirSync(path.dirname(AUDIT_LOG_PATH), { recursive: true }); fs.appendFileSync(AUDIT_LOG_PATH, `${JSON.stringify(record)}\n`, "utf8"); } catch (error) { console.error("Audit log write failed", error.message); } }
function requireInternalSecret(req, res, next) { const secret = process.env.GAMEOPS_INTERNAL_SECRET; if (!secret) return res.status(503).json({ error: "Internal service secret is not configured" }); if (req.get("authorization") !== `Bearer ${secret}`) return res.status(401).json({ error: "Unauthorized" }); next(); }
app.use(express.json({ limit: "1mb" })); app.use(express.static("public", { extensions: ["html"] }));
app.get("/health", (_req,res)=>res.json({ok:true,service:"tryamm",site:SITE_URL}));
app.get("/api/platform/status", (_req,res)=>res.json(PLATFORM_STATUS));
app.get("/api/services", (_req,res)=>res.json(TRYAMM_SERVICES));
app.get("/api/creator-universe", (_req,res)=>res.json(CREATOR_UNIVERSE)); app.get("/api/monetization",(_req,res)=>res.json(MONETIZATION)); app.get("/api/delivery/orchestration",(_req,res)=>res.json(DELIVERY_ORCHESTRATION));
registerEconomicOpportunityRoutes({ app, manifest: ECONOMIC_OPPORTUNITY, manager: economicOpportunity, requireInternalSecret, appendAudit }); registerMobilityOnboardingRoutes({ app, readiness: GLOBAL_MOBILITY_READINESS, manager: mobilityOnboarding, requireInternalSecret, appendAudit }); registerOmniCare360Routes({ app, manifest: OMNICARE_360, manager: omniCare360, requireInternalSecret, appendAudit }); registerHoloCommercialRoutes({ app, manager: holoCommercial, requireInternalSecret, appendAudit }); registerAssetMarketplaceRoutes({ app, manifest: ASSET_HOLOGRAM_MARKETPLACE, manager: assetMarketplace, requireInternalSecret, appendAudit }); registerDnaProtectionRoutes({ app, manifest: DNA_PROTECTION, manager: dnaProtection, requireInternalSecret, appendAudit }); registerCostOpsRoutes({ app, manager: costOps, pricing: ASSET_GENERATION_PRICING, requireInternalSecret, appendAudit }); registerTryAmmAnalyticsRoutes({ app, manager: tryAmmAnalytics, manifest: TRYAMM_ANALYTICS, requireInternalSecret, appendAudit });
app.get("/api/gameverse",(_req,res)=>res.json(GAMEVERSE)); app.get("/api/gameverse/status",(_req,res)=>res.json({platform:GAMEVERSE.platform,gameCount:GAMEVERSE.games.length,productionPlayableCount:GAMEVERSE.games.filter(g=>g.status==='production').length,prototypePlayableCount:1}));
app.post('/api/gameverse/profile',(req,res)=>{try{res.json(gameRuntime.upsertProfile(req.body||{}))}catch(e){res.status(400).json({error:e.message})}}); app.get('/api/gameverse/profile/:id',(req,res)=>{const p=gameRuntime.getProfile(req.params.id);p?res.json(p):res.status(404).json({error:'Profile not found'})}); app.post('/api/gameverse/result',(req,res)=>{const result=gameRuntime.recordResult(req.body||{});appendAudit({event:'gameverse.result',result,at:new Date().toISOString()});res.status(result.accepted?200:409).json(result)}); app.post('/api/gameverse/matchmaking',(req,res)=>{try{res.json(gameRuntime.enqueue(req.body||{}))}catch(e){res.status(400).json({error:e.message})}}); app.get('/api/gameverse/matches/:id',(req,res)=>{const m=gameRuntime.getMatch(req.params.id);m?res.json(m):res.status(404).json({error:'Match not found'})});
app.get("/api/quantum-speed-engine",(_req,res)=>res.json(QUANTUM_SPEED_ENGINE)); app.get("/api/living-world/policy",(_req,res)=>res.json(PLAY_SESSION_POLICY)); app.post("/api/living-world/sessions",(req,res)=>{try{res.status(201).json(playSessions.create(req.body||{}))}catch(e){res.status(400).json({error:e.message})}}); app.get("/api/living-world/sessions/:id",(req,res)=>{const s=playSessions.get(req.params.id);s?res.json(s):res.status(404).json({error:'Session not found'})});
app.get("/api/gameops/policy",(_req,res)=>res.json(GAMEOPS_POLICY)); app.post("/api/gameops/issues",requireInternalSecret,(req,res)=>{const incident={id:crypto.randomUUID(),...req.body,status:'reported',detectedAt:new Date().toISOString()};gameOpsIncidents.unshift(incident);appendAudit({event:'incident.reported',incident});io.emit('gameops:incident',incident);res.status(201).json(incident)}); app.get("/api/gameops/issues",requireInternalSecret,(_req,res)=>res.json({incidents:gameOpsIncidents,persistence:AUDIT_LOG_PATH}));
io.on('connection',socket=>{socket.on('gameverse:join',({matchId,playerId})=>{if(!matchId||!playerId)return;socket.join(`match:${matchId}`);socket.data.playerId=playerId;socket.data.matchId=matchId;socket.to(`match:${matchId}`).emit('gameverse:peer',{playerId,connected:true})});socket.on('gameverse:state',payload=>{const {matchId}=socket.data;if(!matchId||!payload)return;const x=Number(payload.x),y=Number(payload.y);if(!Number.isFinite(x)||!Number.isFinite(y)||x<0||x>800||y<0||y>450)return;socket.to(`match:${matchId}`).emit('gameverse:state',{playerId:socket.data.playerId,x,y,t:Date.now()})});socket.on('disconnect',()=>{if(socket.data.matchId)socket.to(`match:${socket.data.matchId}`).emit('gameverse:peer',{playerId:socket.data.playerId,connected:false})})});
server.listen(process.env.PORT||10000,()=>console.log(`Server running for ${SITE_URL}`));