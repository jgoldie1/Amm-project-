const output=document.querySelector('#workspace');const form=document.querySelector('#alphaForm');const input=document.querySelector('#alphaInput');let tool='workstation';
function esc(v){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}
function show(title,data){output.innerHTML+=`<article class="message assistant"><div class="avatar">A</div><div class="bubble"><strong>${esc(title)}</strong><p>${esc(JSON.stringify(data,null,2))}</p></div></article>`;output.scrollTop=output.scrollHeight;}
async function post(url,body){const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw new Error(d.error||'Request failed');return d;}
document.querySelectorAll('[data-tool]').forEach(b=>b.addEventListener('click',()=>{tool=b.dataset.tool;document.querySelectorAll('[data-tool]').forEach(x=>x.classList.remove('active'));b.classList.add('active');input.focus();}));
form.addEventListener('submit',async e=>{e.preventDefault();const value=input.value.trim()||'TryAMM Demo';try{let result;if(tool==='workstation')result=await post('/api/alpha/workstations',{ownerId:'demo-user',oneHandedMode:true});if(tool==='agent')result=await post('/api/alpha/call-center/agents',{displayName:value,email:'demo@tryamm.online',skills:['creator support','marketplace','accessibility']});if(tool==='queue')result=await post('/api/alpha/call-center/queues',{name:value,channels:['voice','chat','email']});if(tool==='interaction')result=await post('/api/alpha/call-center/interactions',{queueId:'demo-queue',subject:value,channel:'chat'});if(tool==='daw')result=await post('/api/alpha/daw/projects',{title:value,ownerId:'demo-creator',plugins:['EQ','Compressor','Reverb']});if(tool==='shopify')result=await post('/api/alpha/shopify/connect',{shop:value.includes('.')?value:`${value}.myshopify.com`});show(`${tool} result`,result);}catch(error){show('Module error',{error:error.message});}});