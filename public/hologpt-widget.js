'use strict';
(()=>{
  if(window.__tryammHoloGPTLoaded)return;window.__tryammHoloGPTLoaded=true;
  const token=()=>localStorage.getItem('tryamm_token')||'';
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const root=document.createElement('div');root.id='hologptWidgetRoot';
  root.innerHTML=`<button id="hologptFab" class="hologpt-fab" type="button" aria-haspopup="dialog" aria-controls="hologptPanel">HoloGPT</button><section id="hologptPanel" class="hologpt-panel" hidden role="dialog" aria-modal="false" aria-label="HoloGPT assistant"><header><div><strong>HoloGPT</strong><span id="hologptStatus">Checking intelligence…</span></div><button id="hologptClose" type="button" aria-label="Close HoloGPT">×</button></header><div id="hologptMessages" class="hologpt-messages"><article class="hologpt-ai"><b>HoloGPT</b><p>I’m connected to the TRYAMM runtime. Ask me about the app, Holoverse, worlds, creator tools, deployment, routes, or what to fix next.</p></article></div><form id="hologptForm"><label class="sr-only" for="hologptInput">Message HoloGPT</label><textarea id="hologptInput" rows="3" maxlength="12000" placeholder="Ask HoloGPT…"></textarea><button id="hologptSend" type="submit">Send</button></form><p id="hologptHint" class="hologpt-hint"></p></section>`;
  document.body.append(root);
  const panel=root.querySelector('#hologptPanel'),fab=root.querySelector('#hologptFab'),close=root.querySelector('#hologptClose'),form=root.querySelector('#hologptForm'),input=root.querySelector('#hologptInput'),send=root.querySelector('#hologptSend'),messages=root.querySelector('#hologptMessages'),statusEl=root.querySelector('#hologptStatus'),hint=root.querySelector('#hologptHint');
  const sessionId=crypto?.randomUUID?.()||`hs_${Date.now()}_${Math.random().toString(36).slice(2)}`;
  function add(kind,text,meta=''){const a=document.createElement('article');a.className=kind==='user'?'hologpt-user':'hologpt-ai';a.innerHTML=`<b>${kind==='user'?'You':'HoloGPT'}</b><p>${esc(text).replace(/\n/g,'<br>')}</p>${meta?`<small>${esc(meta)}</small>`:''}`;messages.append(a);messages.scrollTop=messages.scrollHeight;}
  async function health(){try{const r=await fetch('/api/hologpt/health',{cache:'no-store'}),d=await r.json();if(!r.ok)throw new Error(d.error||'health failed');statusEl.textContent=d.intelligentProviderConfigured?`Online · ${d.provider}`:'Diagnostic mode · model key needed';statusEl.dataset.ready=d.intelligentProviderConfigured?'true':'false';}catch{statusEl.textContent='Offline';statusEl.dataset.ready='false';}}
  fab.addEventListener('click',()=>{panel.hidden=false;fab.hidden=true;health();setTimeout(()=>input.focus(),0)});close.addEventListener('click',()=>{panel.hidden=true;fab.hidden=false});
  form.addEventListener('submit',async e=>{e.preventDefault();const text=input.value.trim();if(!text)return;const auth=token();add('user',text);input.value='';send.disabled=true;hint.textContent='Thinking…';if(!auth){add('ai','Sign in to TRYAMM first so HoloGPT can use your account and persistent project memory.','Authentication required');hint.innerHTML='<a href="/">Go to TRYAMM home to sign in</a>';send.disabled=false;return;}try{const r=await fetch('/api/hologpt/chat',{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${auth}`},body:JSON.stringify({message:text,page:location.pathname,sessionId})}),d=await r.json();if(!r.ok)throw new Error(d.error||'HoloGPT request failed');add('ai',d.answer,`${d.provider}${d.degraded?' · degraded':''}${d.memorySaved?' · memory saved':''}`);statusEl.textContent=d.degraded?`Diagnostic mode · ${d.provider}`:`Online · ${d.provider}`;hint.textContent='';}catch(error){add('ai',`I could not complete that request: ${error.message}`,'Runtime error');hint.textContent='Check API health or sign in again.';}finally{send.disabled=false;}});
  health();
})();
